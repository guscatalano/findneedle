<Page
    x:Class="FindNeedleUX.Pages.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24">
            <!-- Header -->
            <StackPanel Spacing="8">
                <TextBlock Text="🔍 FindNeedle" FontSize="32" FontWeight="Bold" Foreground="{ThemeResource AccentColorBrush}"/>
                <TextBlock Text="Advanced Log Search &amp; Analysis" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- Quick Actions -->
            <TextBlock Text="Quick Actions" FontWeight="SemiBold" FontSize="18"/>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="📁 New Search" Click="NewSearchButton_Click" Width="160" Height="80" HorizontalAlignment="Left">
                    <Button.Background>
                        <SolidColorBrush Color="{ThemeResource SystemAccentColor}" Opacity="0.1"/>
                    </Button.Background>
                </Button>
                <Button Content="📝 RuleDSL Config" Click="RuleDSLConfigButton_Click" Width="160" Height="80" HorizontalAlignment="Left">
                    <Button.Background>
                        <SolidColorBrush Color="{ThemeResource SystemAccentColor}" Opacity="0.1"/>
                    </Button.Background>
                </Button>
                <Button Content="🖼️ UML Diagram" Click="UMLDiagramButton_Click" Width="160" Height="80" HorizontalAlignment="Left">
                    <Button.Background>
                        <SolidColorBrush Color="{ThemeResource SystemAccentColor}" Opacity="0.1"/>
                    </Button.Background>
                </Button>
            </StackPanel>

            <!-- Recent Pipelines -->
            <TextBlock Text="Recent Pipelines" FontWeight="SemiBold" FontSize="18"/>
            <Border BorderThickness="1" BorderBrush="{ThemeResource SystemControlForegroundAltHighBrush}" Padding="16" CornerRadius="8">
                <StackPanel Spacing="8">
                    <TextBlock Text="No recent pipelines found" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="Create a new pipeline in the RuleDSL Config page" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- Features -->
            <TextBlock Text="Key Features" FontWeight="SemiBold" FontSize="18"/>
            <StackPanel Spacing="8">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <SymbolIcon Symbol="Folder" Foreground="{ThemeResource AccentColorBrush}"/>
                    <TextBlock Text="Plugin-based data sources (folders, files, event logs, ETW)" TextWrapping="Wrap"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <SymbolIcon Symbol="Filter" Foreground="{ThemeResource AccentColorBrush}"/>
                    <TextBlock Text="RuleDSL for filtering, enrichment, and UML generation" TextWrapping="Wrap"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <SymbolIcon Symbol="List" Foreground="{ThemeResource AccentColorBrush}"/>
                    <TextBlock Text="Multiple result viewers (Web, Community, Native)" TextWrapping="Wrap"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <SymbolIcon Symbol="Pictures" Foreground="{ThemeResource AccentColorBrush}"/>
                    <TextBlock Text="PlantUML/Mermaid integration for diagram generation" TextWrapping="Wrap"/>
                </StackPanel>
            </StackPanel>

            <!-- Configuration Note -->
            <Border BorderThickness="1" BorderBrush="{ThemeResource SystemControlBackgroundAccentBrush}" Padding="16" CornerRadius="8" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="⚙️ Configuration" FontWeight="SemiBold"/>
                    <TextBlock Text="Configuration is managed via PluginConfig.json and RuleDSL files. You can control which plugins are loaded, enable registry-based plugin loading, and configure PlantUML integration." TextWrapping="Wrap"/>
                    <HyperlinkButton Content="View Documentation" NavigateUri="https://github.com/guscatalano/findneedle"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
