<Page
    x:Class="FindNeedleUX.Pages.ReformatRulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Field extraction" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"
                       Text="Pull structured values out of dense one-line messages using a regex with named groups (?&lt;Field&gt;...). Built-ins cover DISM and CBS; add your own for any log."/>

            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6"
                    Padding="12" Margin="0,8,0,0" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                <StackPanel Spacing="4">
                    <CheckBox x:Name="EnrichmentEnabledCheck" Content="Store extracted fields as columns (enrichment)"
                              Checked="EnrichmentEnabledCheck_Changed" Unchecked="EnrichmentEnabledCheck_Changed"/>
                    <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"
                               Text="Off: fields show only in the results details panel. On: the rules also run during the scan and write the values into real, filterable/sortable columns (ProcessId, ThreadId, Provider, …) — also visible to exports, diagrams, and the MCP server. The per-row regex is a scan-time cost; enriched scans use a separate cache, so flipping this doesn't force a rescan once both are warm. Applies on the next search."/>
                </StackPanel>
            </Border>
        </StackPanel>

        <Button Grid.Row="1" HorizontalAlignment="Left" Click="Button_AddRule">
            <StackPanel Orientation="Horizontal" Spacing="6">
                <FontIcon Glyph="&#xE710;" FontSize="14"/><TextBlock Text="Add rule"/>
            </StackPanel>
        </Button>

        <Border Grid.Row="2" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="6" Padding="12" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <StackPanel Spacing="6">
                <TextBlock Text="Try it" FontWeight="SemiBold"/>
                <TextBox x:Name="TestInput" PlaceholderText="Paste a log message to see how the enabled rules break it up…"
                         TextWrapping="Wrap" AcceptsReturn="True" Height="60" TextChanged="TestInput_Changed"/>
                <StackPanel x:Name="TestOutput" Spacing="2"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ListHost" Spacing="8"/>
        </ScrollViewer>
    </Grid>
</Page>
