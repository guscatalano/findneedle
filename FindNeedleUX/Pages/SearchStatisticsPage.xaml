<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FindNeedleUX.Pages.SearchStatisticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel MaxWidth="820" Spacing="12">
            <Grid>
                <TextBlock Text="Search statistics &amp; timing" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                    <Button x:Name="RefreshButton" Content="Refresh" Click="Refresh_Click"/>
                    <Button x:Name="CopyButton" Content="Copy" Click="Copy_Click"/>
                </StackPanel>
            </Grid>
            <TextBlock Text="Statistics from the most recent search — records loaded vs. matched, memory, time per phase, and a per-provider/per-file breakdown — plus the timing report (where the wall-clock went)."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>

            <!-- Summary: records / memory / time -->
            <Border Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="6" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock Text="Summary" FontWeight="SemiBold"/>
                    <TextBlock x:Name="SummaryText" FontFamily="Consolas" FontSize="12"
                               IsTextSelectionEnabled="True" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Per-component / provider / file breakdown -->
            <Expander x:Name="BreakdownExpander" Header="Breakdown by component / provider / file"
                      HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <TreeView x:Name="BreakdownTree" AllowDrop="False" SelectionMode="None">
                    <TreeView.ItemTemplate>
                        <DataTemplate>
                            <TreeViewItem Content="{Binding Content}" IsExpanded="True"/>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Expander>

            <!-- Timing report (why so slow?) -->
            <Border Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="6" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock Text="Timing — where the wall-clock went (why so slow?)" FontWeight="SemiBold"/>
                    <TextBlock x:Name="PerfText" FontFamily="Consolas" FontSize="12"
                               IsTextSelectionEnabled="True" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
