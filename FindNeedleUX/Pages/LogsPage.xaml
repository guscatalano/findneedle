<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FindNeedleUX.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10,10,10,0" Spacing="2">
            <TextBlock Text="Logs" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="FindNeedle's own diagnostic log for this run — turn on Debug Logging to capture more detail. (This is the app's log, not the logs you're analyzing.)"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1" Spacing="10">
            <ToggleSwitch x:Name="DebugToggleSwitch" Header="Debug Logging" Toggled="DebugToggleSwitch_Toggled"/>
            <TextBlock x:Name="DebugStatusText" Margin="10,0,0,0" VerticalAlignment="Center"/>
            <Button Content="Copy Selected" Click="CopySelected_Click" VerticalAlignment="Center"/>
            <Button Content="Copy All" Click="CopyAll_Click" VerticalAlignment="Center"/>
            <Button Content="Clear" Click="Clear_Click" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <ListView x:Name="LogListView"
               SelectionMode="Extended"
               IsItemClickEnabled="False"
               Margin="10"
               FontFamily="Consolas"
               FontSize="12"
               ItemsSource="{x:Bind LogLines, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="NoWrap" IsTextSelectionEnabled="True"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock x:Name="EmptyLogsText" Visibility="Collapsed"
                       HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                       TextWrapping="Wrap" MaxWidth="360"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       Text="No log entries yet. Enable Debug Logging to capture more detail."/>
        </Grid>
    </Grid>
</Page>
