<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FindNeedleUX.Pages.PluginConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Click="Back_Click" ToolTipService.ToolTip="Back to plugins">
                    <FontIcon Glyph="&#xE72B;" FontSize="14"/>
                </Button>
                <TextBlock Text="Plugin configuration (DLLs)" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="DLLs FindNeedle loads at startup. Edit, enable/disable, or add plugin assemblies, then Save. Use “Reload all plugins” on the Plugins page to apply."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" FontSize="12" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="6" Padding="8">
            <ListView x:Name="PluginConfigListView" ItemsSource="{x:Bind PluginConfigEntries, Mode=TwoWay}"
                      SelectionMode="Single" SelectedItem="{x:Bind SelectedPluginConfigEntry, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:PluginConfigEntryViewModel">
                        <StackPanel Orientation="Horizontal" Spacing="4" Padding="0,3,0,3">
                            <TextBox Text="{x:Bind Name, Mode=TwoWay}" Width="180" PlaceholderText="Name"/>
                            <TextBox Text="{x:Bind Path, Mode=TwoWay}" Width="340" PlaceholderText="Path to .dll"/>
                            <Button Content="…" Width="32" Click="PickPluginFile_Click" Tag="{x:Bind}"/>
                            <CheckBox IsChecked="{x:Bind Enabled, Mode=TwoWay}" Content="Enabled" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button Content="Add" Click="AddPluginConfigEntry_Click"/>
            <Button Content="Remove" Click="RemovePluginConfigEntry_Click"/>
            <Button Content="Save" Click="SavePluginConfig_Click" Style="{ThemeResource AccentButtonStyle}"/>
            <TextBlock x:Name="SaveStatus" VerticalAlignment="Center" FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Page>
