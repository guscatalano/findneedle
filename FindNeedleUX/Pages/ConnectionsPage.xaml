<Page
    x:Class="FindNeedleUX.Pages.ConnectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="{StaticResource PagePadding}" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock x:Name="PageHeading" Text="Connections" Style="{StaticResource PageHeading}"/>
            <TextBlock Style="{StaticResource PageSubtitle}"
                       Text="Reusable online-source endpoints + credentials (Azure DevOps, GitHub, Kusto). Set one up once here, then pick it when adding a source — no re-entering org / repo / cluster every time."/>
        </StackPanel>

        <!-- Add buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Content="Add Azure DevOps" Click="Button_AddAdo"/>
            <Button Content="Add GitHub" Click="Button_AddGithub"/>
            <Button Content="Add Kusto" Click="Button_AddKusto"/>
        </StackPanel>

        <!-- List -->
        <Border Grid.Row="2" Style="{StaticResource PageCard}" Padding="0" VerticalAlignment="Top">
            <Grid>
                <TextBlock x:Name="EmptyHint" Margin="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Text="No connections yet. Add one above." Visibility="Collapsed"/>
                <ListView x:Name="ConnList" Padding="4" SelectionMode="None"/>
            </Grid>
        </Border>
    </Grid>
</Page>
