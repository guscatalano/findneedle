<Page
    x:Class="FindNeedleUX.Pages.ConnectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Connections" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"
                       Text="Reusable online-source endpoints + credentials (Azure DevOps, GitHub, Kusto). Set one up once here, then pick it when adding a location — no re-entering org / repo / cluster every time."/>
        </StackPanel>

        <!-- Add buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Click="Button_AddAdo">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="🔷"/><TextBlock Text="Add Azure DevOps"/>
                </StackPanel>
            </Button>
            <Button Click="Button_AddGithub">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="🐙"/><TextBlock Text="Add GitHub"/>
                </StackPanel>
            </Button>
            <Button Click="Button_AddKusto">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <TextBlock Text="🔎"/><TextBlock Text="Add Kusto"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- List -->
        <Border Grid.Row="2" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <TextBlock x:Name="EmptyHint" Margin="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Text="No connections yet. Add one above." Visibility="Collapsed"/>
                <ListView x:Name="ConnList" Padding="4" SelectionMode="None"/>
            </Grid>
        </Border>
    </Grid>
</Page>
