<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FindNeedleUX.Pages.AutoAddRulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="24" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Auto-add rules" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Rules that get applied to a search automatically when their condition matches the logs you load (file type, source kind, path pattern, provider, or build). Toggle individual rules below, or turn the whole feature off."/>
        </StackPanel>

        <!-- Master switch + actions -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ToggleSwitch x:Name="MasterSwitch" Grid.Column="0"
                          Header="Auto-add rules to searches"
                          OnContent="On" OffContent="Off"
                          Toggled="MasterSwitch_Toggled"/>
            <Button Grid.Column="1" Content="Add rule file…" Click="AddFile_Click" VerticalAlignment="Bottom"/>
            <Button Grid.Column="2" Content="Reveal library folder" Click="RevealLibrary_Click" VerticalAlignment="Bottom"/>
        </Grid>

        <InfoBar x:Name="StatusBar" Grid.Row="2" IsOpen="False" IsClosable="True"/>

        <Border Grid.Row="3" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="6">
            <ListView x:Name="EntriesList" SelectionMode="None"
                      ScrollViewer.HorizontalScrollMode="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="12,8"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:AutoRuleRow">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ToggleSwitch Grid.Column="0" VerticalAlignment="Center"
                                          IsOn="{x:Bind Enabled}"
                                          MinWidth="0" OnContent="" OffContent=""
                                          Toggled="EntryToggle_Toggled" Tag="{x:Bind Id}"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <Border Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}"
                                            CornerRadius="3" Padding="6,1" VerticalAlignment="Center"
                                            Visibility="{x:Bind BuiltInVisibility}">
                                        <TextBlock Text="Built-in" FontSize="11"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{x:Bind Summary}" FontSize="12" TextWrapping="Wrap"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind RulePath}" FontSize="11" FontFamily="Consolas"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                            </StackPanel>
                            <Button Grid.Column="2" Content="Edit condition…" Click="Edit_Click" Tag="{x:Bind Id}"
                                    VerticalAlignment="Center"/>
                            <Button Grid.Column="3" Content="Remove" Click="Remove_Click" Tag="{x:Bind Id}"
                                    VerticalAlignment="Center" IsEnabled="{x:Bind CanRemove}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
