<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FindNeedleUX.Pages.SystemInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Padding="{StaticResource PagePadding}" MaxWidth="1000" HorizontalAlignment="Left" Spacing="12">
            <StackPanel Spacing="4">
                <TextBlock x:Name="PageHeading" Text="System check" Style="{StaticResource PageHeading}"/>
                <TextBlock Text="Environment and the external tools FindNeedle relies on. ✓ ready · ✗ missing · • info"
                           Style="{StaticResource PageSubtitle}"/>
            </StackPanel>

            <Border Style="{StaticResource PageCard}" Padding="8">
                <StackPanel x:Name="HealthList" Spacing="2"/>
            </Border>

            <Button x:Name="RefreshButton" Content="Re-check" Click="Refresh_Click"/>
        </StackPanel>
    </ScrollViewer>
</Page>
