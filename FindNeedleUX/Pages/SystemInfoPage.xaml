<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FindNeedleUX.Pages.SystemInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FindNeedleUX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="24">
        <StackPanel MaxWidth="720" Spacing="8">
            <TextBlock Text="System Check" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock Text="Environment and the external tools FindNeedle relies on. ✓ ready · ✗ missing · • info"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,8"/>

            <Border Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="6" Padding="8">
                <StackPanel x:Name="HealthList" Spacing="2"/>
            </Border>

            <Button x:Name="RefreshButton" Content="Re-check" Click="Refresh_Click" Margin="0,8,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
